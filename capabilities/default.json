{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Live Interpreter desktop control app.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
